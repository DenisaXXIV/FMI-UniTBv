#include <stdio.h>
#include <conio.h>
#include <ctype.h>

#define suma(a,b) a+b
#define max(a, b) a>b ? a : b
#define concat(a,b) a##b

char c;
int n;
int v;

void main()
{
	v = 18;
	/*
	c = _getch();
	scanf_s("%c", &c);
	printf("%c", toupper(c));
	
	if (v-- >= 18);
	{
		printf("Da");
	}
	else
	{
		printf("Nu");
	}
	*/

	printf("%d", max(1, 2));

	printf("")

}